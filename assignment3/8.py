# A list of numbers are stored as a list. Write a program which will print all the
# numbers and when the list will be exhausted, it will print “No more items”.

D = [1, 2, 4]
for i in D:
      print(i)
print("No more items ")